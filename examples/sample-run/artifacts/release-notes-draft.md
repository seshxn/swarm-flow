# Release Notes Draft

Admins can bulk reassign open cases by region when they have the required region-level permission. Each reassigned case records an audit event with the actor, previous assignee, new assignee, timestamp, and region.

Rollout should start behind a feature flag while audit volume and partial failure behavior are monitored.
