# Jira Connector

Planned capabilities:

- search relevant issues
- preview issue creation and updates
- create or update issues with idempotency keys after approval
- write sync reports into run artifacts

The default policy requires preview mode before Jira writes.
