# Filesystem Connector

Planned capabilities:

- read local repository files
- search project context
- write artifacts into `.runs/`
- validate path boundaries before writes

Filesystem writes should be limited to the run workspace unless explicitly requested.
