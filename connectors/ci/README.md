# CI Connector

Planned capabilities:

- read CI status
- fetch failed job summaries
- attach CI evidence to validation artifacts
- trigger repair loop hooks on failure

CI repair loops should not retry indefinitely.
