# Tests

The primary test suites live next to the packages they validate:

- `packages/core/test/`
- `packages/runtime/test/`
- `packages/connectors/test/`
- `packages/cli/test/`

Run all tests with:

```bash
npm test
```
